/* eslint-disable */
// const res = (x) => args[0] * x ** 2 + args[1] * x + args[2];

function makeTuple(first, second) {
  return [first, second];
}

const tuple = makeTuple(true, 'asdf');
console.log(tuple);
